packages:
  - "packages/*"

# pnpm >=9 blocks dependency build scripts by default. argon2 is a native module
# that must compile on install; allow only the build scripts we trust.
onlyBuiltDependencies:
  - argon2
  - esbuild
